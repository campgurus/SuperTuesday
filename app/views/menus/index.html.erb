<h1>List of menus:</h1>

<ul>
	<% Menu.all.each do |menu| %>
	  <li>
			<%= link_to menu.date, menu_path(menu) %> 
	  </li>

	<% end %>
</ul>

<%= link_to 'Create a New Menu', new_menu_path, :class => "btn btn-success" %>